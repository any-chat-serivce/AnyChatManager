# Anychat Manager PHP SDK

Anychat Manager PHP SDK is a PHP library for interacting with the Anychat Manager API.

## Installation
```apacheconf
composer require any-chat-manager/supports
```

follow `test.php` for usage